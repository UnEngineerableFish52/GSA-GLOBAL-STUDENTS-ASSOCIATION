name: gsa_flutter
description: GSA - Global Students Association Flutter app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Network
  dio: ^5.4.0
  socket_io_client: ^2.0.3+1
  
  # State management
  provider: ^6.1.1
  
  # UI
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  
  # Storage
  shared_preferences: ^2.2.2
  
  # Utils
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  # Placeholder assets - replace with actual icons when ready
  # assets:
  #   - assets/icons/
  #   - assets/images/
